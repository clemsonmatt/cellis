<!DOCTYPE html>
<html>
  <head>
    <title>cellis</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <style>
        /*
         * Base structure
         */
        .sub-header {
          padding-bottom: 10px;
          border-bottom: 1px solid #eee;
        }

        /*
         * Top navigation
         * Hide default border to remove 1px line.
         */
        .navbar-fixed-top {
          border: 0;
        }

        /*
         * Sidebar
         */

        /* Hide for mobile, show later */
        .sidebar {
          display: none;
        }
        @media (min-width: 768px) {
          .sidebar {
            position: fixed;
            top: 107px;
            bottom: 0;
            left: 0;
            z-index: 1000;
            display: block;
            padding: 20px;
            overflow-x: hidden;
            overflow-y: auto; /* Scrollable contents if viewport is shorter than content. */
            background-color: #fff;
            border-right: 1px solid #eee;
            font-size: 1.2em;
          }
        }

        /* Sidebar navigation */
        .nav-sidebar {
          margin-right: -21px; /* 20px padding + 1px border */
          margin-bottom: 20px;
          margin-left: -20px;
          margin-top: -20px;
        }
        .nav-sidebar > li {
          display: block;
          width: 100%;
          height: 60px;
          /*line-height: 3.4em;*/
        }
        .nav-sidebar > li > a {
          padding: 18px 35px;
          /*padding-right: 35px;
          padding-left: 35px;*/
          display: block;
          height: 100%;
          width: 100%;
          text-decoration: none;
        }
        .nav-sidebar > li:hover,
        .nav-sidebar > li:focus {
          background-color: #1f9bcf;
        }
        .nav-sidebar > .active > a,
        .nav-sidebar > .active > a:hover,
        .nav-sidebar > .active > a:focus,
        .nav-sidebar > li:hover > a,
        .nav-sidebar > li:focus > a {
          color: #fff;
          background-color: #1f9bcf;
          text-decoration: none;
        }


        /*
         * Main content
         */

        .main {
          padding: 20px;
        }
        @media (min-width: 768px) {
          .main {
            padding-right: 40px;
            padding-left: 40px;
          }
        }
        .main .page-header {
          margin-top: 0;
          margin-left: -40px;
          margin-right: -40px;
          padding: 0px 40px 30px 40px;
          border-bottom: 1px solid #eee;
        }


        /* DataTables */
        .pagination {
          float: right;
        }
        .dataTables_filter {
          float: right;
        }

    </style>
  </head>

  <body class="bg-light">
      <div id="container">
          <div class="bg-white">
            <%= render 'layouts/nav_manage' %>
          </div>

          <div class="container-fluid">
              <div class="row">
                  <div class="col-sm-2 sidebar">
                      <%= yield :sidebar %>
                  </div>

                  <div class="col-md-10 offset-md-2 main">
                      <h1 class="font-market-saturday-lg" style="border-bottom: 1px solid #eee;">
                          <%= yield :page_header %>
                      </h1>

                      <div style="margin-top: 30px;">
                          <%= yield %>
                      </div>
                  </div>
              </div>
          </div>
      </div>
  </body>
</html>
